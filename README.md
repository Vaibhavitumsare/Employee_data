# Employee_data
A full-stack Employee Management System built with Spring Boot (backend) and HTML, CSS, JavaScript (frontend). Supports CRUD operations (add, update, delete) on employee records and search by ID/name. Deployed on cloud platforms like Heroku (backend) and Netlify (frontend) for easy access and management.
